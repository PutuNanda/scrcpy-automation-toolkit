# scrcpy-automation-toolkit
This project is designed to simplify the process of connecting Android devices to scrcpy using multiple methods, such as USB or network connections, and to automatically reconnect when the connection is unexpectedly lost.
